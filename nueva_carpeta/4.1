#include<iostream>
#include<vector>
using namespace std;

int main() {
    int n;
    cout << "ingrese el tamaño que quiere: ";
    cin >> n;
    vector<int> arreglo(n);  
    for (int i = 0; i < n; i++) {
        cout << "Ingrese el elemento " << i << ": ";
        cin >> arreglo[i];
    }
    
        int suma = 0;
    
        for (int num : arreglo){
            suma += num;
    }
            cout<< "la suma de los valores da : " << suma << endl;
        return 0;  
} 4.1
